{
  "name": "@digivorefr/asterix-sass",
  "version": "1.0.0",
  "description": "The scss framework who doesn't think you're a noob",
  "main": "src/index.scss",
  "repository": "git@github.com:digivorefr/asterix.git",
  "author": "Digivore",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.15",
    "sass": "^1.49.0"
  },
  "scripts": {
    "clean": "rm -rf playground/compiled-css",
    "dev": "yarn && yarn run clean && sass --watch playground/scss/:playground/compiled-css/",
    "build": "yarn && yarn run clean && sass --no-source-map playground/scss/:playground/compiled-css/",
    "play": "concurrently \"nodemon playground/server.js\" \"yarn run dev\""
  },
  "dependencies": {
    "express": "^4.17.2",
    "fs-extra": "^10.0.0"
  }
}